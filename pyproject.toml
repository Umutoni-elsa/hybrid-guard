[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hybrid-guard"
version = "0.1.0"
description = "Lightweight, two-stage prompt injection detection system"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
